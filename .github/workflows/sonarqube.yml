name: SonarCloud Sample Scan

on:
  workflow_dispatch:  # allows manual triggering
  push:
    branches:
      - main  # adjust if your default branch is different

jobs:
  sonarcloud:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'  # ensure TensorFlow compatibility

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov

      - name: Run tests and generate coverage report
        run: |
          pytest Deployment/ --cov=Deployment.sample --cov-report=xml --cov-report=term

      - name: Download and run SonarScanner
        run: |
          curl -sSLo sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.8.0.2856-linux.zip
          unzip sonar-scanner.zip
          ./sonar-scanner-4.8.0.2856-linux/bin/sonar-scanner \
            -Dsonar.projectKey=Group6 \
            -Dsonar.organization=Steve8695 \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.sources=Deployment \
            -Dsonar.inclusions=Deployment/sample.py \
            -Dsonar.python.coverage.reportPaths=coverage.xml \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}

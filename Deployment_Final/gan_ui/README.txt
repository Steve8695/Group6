GAN Streamlit UI

1. Install requirements:
   pip install -r requirements.txt

2. Make sure your GAN backend is running at http://localhost:8000

3. Run this UI:
   streamlit run streamlit_app.py

4. Open your browser to http://localhost:8501

Change BACKEND_URL in streamlit_app.py if your backend runs on a different IP/port.
